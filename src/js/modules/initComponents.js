// initComponents.js
import { initMultipleCustomSelect } from '@components/forms';

export function initComponents(context = document) {
    initMultipleCustomSelect(context);
    // afegeix aquí la inicialització d’altres mòduls:
    // initTooltip(context);
    // initDatePicker(context);
}